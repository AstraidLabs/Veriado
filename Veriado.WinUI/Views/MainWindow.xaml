<Window
    x:Class="Veriado.WinUI.Views.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:contracts="using:Veriado.Contracts.Search"
    xmlns:conv="using:CommunityToolkit.WinUI.Converters"
    xmlns:i="using:Microsoft.Xaml.Interactivity"
    xmlns:ctb="using:CommunityToolkit.WinUI.Behaviors"
    xmlns:winui="using:Microsoft.UI.Xaml.Controls"
    Title="Veriado"
    Width="1280"
    Height="800">

    <Window.Resources>
        <conv:BooleanToVisibilityConverter x:Key="BoolToVisibility" />
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="280" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <winui:CommandBar Grid.Row="0" Grid.ColumnSpan="3">
            <winui:AppBarButton Icon="Refresh" Label="Obnovit"
                                Command="{Binding Files.RefreshCommand}" />
            <winui:AppBarSeparator />
            <winui:AppBarButton Icon="Find" Label="Spotlight"
                                Command="{Binding Search.OpenCommand}" />
        </winui:CommandBar>

        <winui:NavigationView x:Name="ShellNavigationView"
                              Grid.Row="1"
                              Grid.Column="0"
                              IsSettingsVisible="True"
                              SelectedItem="{Binding SelectedNavItem, Mode=TwoWay}">
            <i:Interaction.Behaviors>
                <ctb:EventToCommandBehavior EventName="SelectionChanged"
                                            Command="{Binding NavigateCommand}"
                                            CommandParameter="{Binding SelectedItem.Tag, ElementName=ShellNavigationView}" />
            </i:Interaction.Behaviors>
            <winui:NavigationView.MenuItems>
                <winui:NavigationViewItem Content="Soubory" Icon="Folder" Tag="Files" IsSelected="True" />
                <winui:NavigationViewItem Content="Import" Icon="Upload" Tag="Import" />
                <winui:NavigationViewItem Content="Nastavení" Icon="Setting" Tag="Settings" />
            </winui:NavigationView.MenuItems>
        </winui:NavigationView>

        <ContentPresenter Grid.Row="1" Grid.Column="1"
                          Content="{Binding CurrentContent}" />

        <Grid Grid.Row="1" Grid.Column="2" MinWidth="360">
            <VisualStateManager.VisualStateGroups>
                <VisualStateGroup>
                    <VisualState x:Name="Narrow">
                        <VisualState.StateTriggers>
                            <AdaptiveTrigger MinWindowWidth="0" />
                        </VisualState.StateTriggers>
                        <VisualState.Setters>
                            <Setter Target="RightPane.Visibility" Value="Collapsed" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="Wide">
                        <VisualState.StateTriggers>
                            <AdaptiveTrigger MinWindowWidth="1200" />
                        </VisualState.StateTriggers>
                        <VisualState.Setters>
                            <Setter Target="RightPane.Visibility" Value="Visible" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateManager.VisualStateGroups>

            <Border x:Name="RightPane" Padding="12">
                <ContentPresenter Content="{Binding CurrentDetail}" />
            </Border>
        </Grid>

        <winui:InfoBar Grid.Row="2" Grid.ColumnSpan="3"
                       IsOpen="{Binding IsInfoBarOpen, Mode=TwoWay}"
                       Message="{Binding StatusMessage}" />

        <Grid Grid.RowSpan="3" Grid.ColumnSpan="3"
              Background="{ThemeResource LayerOnAcrylicFillColorDefaultBrush}"
              Visibility="{Binding Search.IsOpen, Converter={StaticResource BoolToVisibility}}">
            <Border MaxWidth="900" Margin="100"
                    HorizontalAlignment="Center" VerticalAlignment="Top"
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    CornerRadius="12" Padding="16">
                <StackPanel Spacing="8">
                    <AutoSuggestBox PlaceholderText="Hledat..."
                                    QueryIcon="Find"
                                    Text="{Binding Search.QueryText, Mode=TwoWay}"
                                    ItemsSource="{Binding Search.Suggestions}"
                                    QuerySubmitted="{x:Bind ViewModel.Search.OnQuerySubmitted}"
                                    SuggestionChosen="{x:Bind ViewModel.Search.OnSuggestionChosen}" />
                    <winui:ListView ItemsSource="{Binding Search.Results}">
                        <winui:ListView.ItemTemplate>
                            <DataTemplate x:DataType="contracts:SearchHitDto">
                                <StackPanel Spacing="4">
                                    <TextBlock Text="{x:Bind Title}" FontWeight="SemiBold" />
                                    <TextBlock Text="{x:Bind Snippet}" TextWrapping="Wrap" />
                                </StackPanel>
                            </DataTemplate>
                        </winui:ListView.ItemTemplate>
                    </winui:ListView>
                </StackPanel>
            </Border>
            <i:Interaction.Behaviors>
                <ctb:EventToCommandBehavior EventName="KeyDown"
                                            Command="{Binding Search.CloseOnEscCommand}" />
            </i:Interaction.Behaviors>
        </Grid>
    </Grid>
</Window>
